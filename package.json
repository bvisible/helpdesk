{
	"private": true,
	"scripts": {
		"postinstall": "cd desk && yarn install",
		"dev": "cd desk && yarn dev",
		"build": "if [ -d 'helpdesk/public/desk/assets' ] && [ -z \"$FORCE_REBUILD\" ]; then echo '[skip-build] pre-built artifacts present at helpdesk/public/desk/assets (set FORCE_REBUILD=1 to force rebuild)'; else yarn build:force; fi",
		"build:force": "cd desk && yarn build"
	},
	"resolutions": {
		"cheerio": "1.0.0-rc.12",
		"prosemirror-model": "1.25.4"
	}
}
